# Bitácora de Trabajo - Período del 15/05/2026 al 14/06/2026 (Verificada según modificaciones de archivos)

A continuación se detalla la tabla de actividades formateada de acuerdo a los requerimientos del programa de formación para su fácil copiado a plantillas de reporte de prácticas o bitácoras. Las fechas y actividades cubren de manera continua todo el período desde el 15/05/2026 hasta el 14/06/2026, coincidiendo con la fecha real de los commits, modificaciones de archivos y el despliegue al servidor de producción.

| Descripción de la actividad<br>(Ingrese cuantas filas sean necesarias) | Competencias del programa de formación aplicadas en el desarrollo de la actividad | Fecha de inicio<br>(dd/mm/aa) | Fecha de fin<br>(dd/mm/aa) | documento, proceso, producto, entregable u otro<br>En anexo puede fortalecer la evidencia si es el caso. | dificultades presentadas, y/o comentarios realizados por el aprendiz y/o jefe inmediato |
| :--- | :--- | :---: | :---: | :--- | :--- |
| **Normalización de Centros de Costo por Empresa, Migración a Excel y Depuración de Código Backend**<br><br>Diseño y ejecución del nuevo modelo relacional `CentroCosto` por empresa para garantizar la restricción multi-tenancy del sistema. Vinculación de usuarios mediante llave foránea a centro de costo e implementación de propiedad `@property centro_costos` en la clase `User` para conservar compatibilidad con el JSON devuelto a React. Remoción completa del formato y de todos los botones de exportación a CSV, migrando el sistema a formato exclusivo de Microsoft Excel (`.xlsx`). Corrección de bugs críticos de atributos no definidos en `counters.py` y `dashboard.py` detectados en pruebas locales. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 15/05/26 | 29/05/26 | **Entregable / Código / Documento:** Tabla relacional `centro_costos` y archivo de migración `014_normalizar_centros_costo.sql`. Refactor de ORM en `models.py` y `repository.py`. Remoción de endpoints y botones CSV en 10 archivos (incluyendo `export.py` y `exportService.ts`). Script de verificación de multi-tenancy `test_multitenant_centro_costos.py`. Documentación técnica: `MIGRACION_CENTROS_COSTO_29_MAYO_2026.md` y `RESUMEN_TRABAJO_26_29_MAYO_2026.md`. | Se presentaron discrepancias locales de importación de módulos y warnings por parte del analizador de código estático (Pylance) al buscar dependencias y módulos que residen exclusivamente dentro de los contenedores Docker. Se resolvió configurando rutas adicionales del workspace local en settings de VS Code. |
| **Monitoreo de Suministros vía HTTP, Simetría del Dashboard, Precisión en Cierres y Seguridad de la API**<br><br>Desarrollo de un extractor de datos web (web scraper) en el backend para realizar consultas a la interfaz interna de Ricoh (*Web Image Monitor*) a través de HTTP (`getStatus.cgi`) como alternativa ante bloqueos de SNMP (puerto 161). Reestructuración del panel de flota en el Dashboard para ocultar barras de tóner de color (C, M, Y) en equipos monocromáticos y expandir la barra K (Negro) a ancho completo. Refactorización del módulo de cierres en `close_service.py` para obtener consumos directamente desde los contadores generales de hardware (`ContadorImpresora`), resolviendo el conflicto de ámbito (*scope shadowing*). Inhabilitación de la columna redundante "B/N" en la tabla simplificada de monocromáticas. Implementación de un middleware de autenticación básica (Basic Auth) con protección contra ataques de temporización (`secrets.compare_digest`) para proteger la documentación interactiva de la API (/docs, /redoc, /openapi.json). | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 01/06/26 | 11/06/26 | **Entregable / Código / Documento:** Módulo de extracción web `toner_parser.py` con autenticación Base64. Lógica de cálculo de cierres corregida en `backend/services/close_service.py` y middleware de autenticación en `main.py`. Componentes frontend modificados `PrinterCard.tsx`, `OverviewDashboard.tsx`, `TablaComparacionSimplificada.tsx` y `CentroCostosAutocomplete.tsx`. Documentación técnica: `RESUMEN_TRABAJO_01_03_JUNIO_2026.md`, `RESUMEN_TRABAJO_11_JUNIO_2026.md` y `FIX_CONTADORES_Y_SUMINISTROS_JUNIO_2026.md`. | Los equipos de la flota tenían SNMP capado en la red local y se presentaban discrepancias matemáticas entre los consumos individuales sumados de usuarios y la diferencia física de páginas del equipo (por impresiones locales sin código). Se solucionó desarrollando el parseador HTTP para leer Web Image Monitor de forma paralela y obteniendo los totales directamente del hardware del equipo en los cierres. |
| **Optimización de Sincronización WIM, Gestión de Trabajos, Encriptación de Contraseñas y Despliegue en Servidor**<br><br>Optimización de la sincronización de usuarios desde WIM Ricoh por lotes (`fastList` en cgi) para reducir el tiempo de descarga a 2 segundos con Lazy Loading y persistencia de cookies en Redis. Desarrollo e integración de la funcionalidad para cancelar/eliminar trabajos de la cola de impresión directamente desde la interfaz web de la aplicación. Almacenamiento encriptado de las contraseñas individuales de administración de impresoras e incremento al 999 del límite de intentos de login fallidos. Ejecución del despliegue en el servidor de producción `192.168.91.131` utilizando el script `deploy_to_server131.py` para levantar contenedores Docker, reconfigurando Nginx para servir la aplicación por HTTP plano (puerto 80 frontend y puerto 8000 backend API) y configurando el repositorio Git remoto y permisos en el servidor para sincronización. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 12/06/26 | 14/06/26 | **Entregable / Código / Base de Datos / Servidor:** Lógica `fastList` y caché de cookies de Ricoh en Redis en `ricoh_web_client.py`. Endpoint `DELETE /api/v1/printers/{printer_id}/jobs/{job_id}` y vista en `PrintJobsPage.tsx`. Tabla modificada por migración `019_add_printer_admin_password.sql`. Script `deploy_to_server131.py`, configuración Nginx del servidor y Git hooks. Herramienta de sincronización inversa `pull_from_server131.py`. Documentación técnica: `RECONFIGURACION_SERVIDOR_131.md` y `RESUMEN_TRABAJO_12_JUNIO_2026.md`. | Posterior al despliegue, la interfaz arrojó errores de CORS debido a la falta de inicialización de atributos de Stored Jobs y un bloqueo inicial de contraseña del administrador. Se solucionó actualizando los módulos web, ejecutando el despliegue con las credenciales respectivas y re-inicializando la contraseña del superusuario mediante el script de base de datos. |
| **Búsqueda de Datos vía Telnet, Borrado Lógico de Usuarios y Desactivación de Permisos en Paralelo**<br><br>Investigación y escaneo de puertos Telnet (puerto 23) en la flota de impresoras de red locales para intentar recolectar logs de conexión, resultando en credenciales restringidas por hardware/firmware. Desarrollo e integración en backend y frontend de la función de eliminación de usuarios: al borrar un usuario, el backend deshabilita de forma concurrente (en paralelo con ThreadPoolExecutor) todos los permisos del usuario en cada impresora asociada mediante peticiones WIM AJAX y realiza un borrado lógico en la base de datos (is_active=False en users y user_printer_assignments). Corrección del flujo de visualización en frontend para permitir buscar y visualizar usuarios inactivos o eliminados. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 18/06/26 | 18/06/26 | **Entregable / Código / Base de Datos / Servidor:** Modificación de `users.py` (deactivación paralela en delete_user) y `ricoh_web_client.py` (parámetro set_password). Botón "Eliminar" e integración en `FilaUsuario.tsx`, `TablaUsuarios.tsx`, y `AdministracionUsuarios.tsx` en React. Documento resumen de desarrollo: `RESUMEN_TRABAJO_18_JUNIO_2026.md`. | Al realizar la deactivación paralela física de permisos, la impresora intentaba reconfigurar la contraseña de carpeta SMB resultando en advertencias/errores 404 controlados. Se solucionó añadiendo el parámetro opcional `set_password=False` al cliente web de Ricoh para omitir el flujo de contraseña en la deactivación. En el frontend, se detectó que los usuarios inactivos quedaban invisibles por filtros cruzados de estado y sincronización; se corrigió omitiendo el filtro de estado activo durante búsquedas e integrando controles de filtrado de Estado DB (Activos/Inactivos/Todos) en el panel de administración. |
