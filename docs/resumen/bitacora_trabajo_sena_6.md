# Bitácora de Trabajo - Período del 15/06/2026 al 14/07/2026 (Verificada según modificaciones de archivos)

A continuación se detalla la tabla de actividades formateada de acuerdo a los requerimientos del programa de formación para su fácil copiado a plantillas de reporte de prácticas o bitácoras. Las fechas y actividades cubren de manera continua todo el período desde el 15/06/2026 hasta el 14/07/2026, coincidiendo con la fecha real de los commits, modificaciones de archivos y el despliegue al servidor de producción.

| Descripción de la actividad<br>(Ingrese cuantas filas sean necesarias) | Competencias del programa de formación aplicadas en el desarrollo de la actividad | Fecha de inicio<br>(dd/mm/aa) | Fecha de fin<br>(dd/mm/aa) | documento, proceso, producto, entregable u otro<br>En anexo puede fortalecer la evidencia si es el caso. | dificultades presentadas, y/o comentarios realizados por el aprendiz y/o jefe inmediato |
| :--- | :--- | :---: | :---: | :--- | :--- |
| **Corrección de IndentationError, Aislamiento Thread-Local de Sesiones WIM y Pruebas de Integración en 5 Impresoras**<br><br>Corrección de un error sintáctico de indentación de 239 líneas en el método `_set_user_functions_internal` surgido tras la refactorización anterior. Implementación del aislamiento thread-local de sesiones HTTP en `RicohWebClient` para prevenir la contaminación de cookies de sesión WIM entre hilos concurrentes al sincronizar permisos en paralelo. Ejecución de pruebas de integración automatizadas sobre 5 impresoras de la red simultáneamente (`.250`, `.251`, `.252`, `.253` y `.110.250`), validando aprovisionamiento, lectura de permisos físicos, edición y desactivación lógica con restauración final del usuario de prueba. Reducción del tiempo de aprovisionamiento de ~240s a 110 segundos para 4 impresoras. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 23/06/26 | 23/06/26 | **Entregable / Código / Servidor:** Corrección sintáctica en `ricoh_web_client.py`. Script de prueba de integración `scratch_comprehensive_test_all_printers.py`. Módulos desplegados en caliente: `ricoh_web_client.py`, `ricoh_password_flow.py`, `retry_strategy.py`, `discovery.py`, `counters.py`. Documentación técnica: `RESUMEN_TRABAJO_23_JUNIO_2026.md` y `FIX_APROVISIONAMIENTO_CONCURRENTE_Y_TIEMPOS_WIM.md`. | Se detectaron conflictos de cookies de sesión WIM entre hilos de sincronización concurrentes. La impresora se bloqueaba cuando dos workers intentaban iniciar sesión y manipular un usuario simultáneamente. Se solucionó instanciando sesiones HTTP (`requests.Session`) thread-local aisladas por impresora, evitando la reutilización de cookies y tokens entre hilos. |
| **Detección de Deriva, Bucle de Reintentos BUSY/TIMEOUT, Auto-guardado de Permisos, Seguridad de Producción y Limpieza de console.log**<br><br>Implementación de la lógica de detección de deriva: si un usuario inactivo en la base de datos se encuentra con permisos activos en la impresora física, el sistema lo reactiva automáticamente. Desarrollo del auto-guardado inmediato en la base de datos PostgreSQL al consultar permisos en vivo (`/discovery/user-details`). Integración de un bucle de reintento automático (4 intentos, 5.0 segundos de espera) en los workers de aprovisionamiento, actualización y desactivación de usuarios. Restauración del endpoint de diagnósticos en vivo (`/discovery/printer/{id}/live-diagnostics`). Limpieza de todas las trazas `console.log` del código fuente del frontend y configuración de Vite/esbuild para remover automáticamente sentencias de depuración en la compilación de producción. Ejecución de la suite de seguridad REST & WebSockets (26 Passed / 0 Failed). | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 26/06/26 | 01/07/26 | **Entregable / Código / Servidor / Pruebas:** Modificaciones en `api/provisioning.py`, `api/users.py` y `api/discovery.py` (bucle de reintentos, detección de drift, auto-guardado). Configuración de `vite.config.ts` (drop `console` y `debugger`). Limpieza de 10 archivos de servicios y componentes. Documentación técnica: `RESUMEN_TRABAJO_01_JULIO_2026.md`. Reportes de pruebas: `test_encryption_service.py`, `test_jwt_service.py`, `test_auth_endpoints.py`, `test_sanitization_service.py`, `security_validation_suite.py`. | Cuando el navegador o el scraper consultaba / modificaba permisos en impresoras físicamente ocupadas por otros usuarios, el dispositivo retornaba el error "Este dispositivo está siendo utilizado por otras funciones". Se resolvió con el bucle de reintentos y con la validación estricta de booleanos (`res is True`) en las respuestas del cliente Ricoh. |
| **Refactorización UX de Administración de Usuarios: Filtros de Estado, Deduplicación de Asignaciones, Interceptor JWT, Modales React y Renderizado de Desactivaciones**<br><br>Reestructuración completa de la página de administración de usuarios con filtros de estado Toggle (Activos / Inactivos) deseleccionables. Deduplicación de 5 registros duplicados en la tabla `user_printer_assignments` en producción mediante script. Migración de la selección de impresora a basarse en `assignment.id` para prevenir colisiones entre impresoras del mismo modelo. Implementación del interceptor de refresco automático de JWT para renovar tokens de acceso transparentemente ante errores 401, eliminando redirecciones abruptas al login. Eliminación del `createPortal` que causaba React Error #31 (bloqueando todos los clicks). Reemplazo del `window.confirm` nativo por un modal de confirmación reactivo para desactivaciones de usuarios. Consolidación de iconos y conteo de permisos desde impresoras asignadas con badge gris de estado inactivo para usuarios sin permisos activos. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 02/07/26 | 03/07/26 | **Entregable / Código / Base de Datos / Documentación:** Script de deduplicación `deployment/deduplicate_assignments.py`. Modificaciones en `useUsuarioStore.ts`, `ModificarUsuario.tsx`, `FilaUsuario.tsx`, `TablaUsuarios.tsx`, `AdministracionUsuarios.tsx`, `apiClient.ts`, `models.py`, `schemas.py`. Documentación técnica: `FIX_UX_ADMINISTRACION_USUARIOS_02_JULIO_2026.md`, `REPORTE_LIMPIEZA_PERMISOS_INACTIVOS_PRODUCCION.md` y `FIX_CONTADOR_IMPRESORAS_Y_RENDER_DESACTIVACION_06_JULIO_2026.md`. CHANGELOG v2.7.0. | Se presentaron asignaciones duplicadas en la base de datos del servidor por operaciones anteriores de `fast_list` que no deduplicaban antes de insertar. Esto provocaba que al alternar entre impresoras iguales, la selección saltaba a la impresora incorrecta. Se solucionó con el script de deduplicación y migrando la lógica de selección a basarse en el ID único de la asignación. |
| **Desactivación Lógica Preservando entry_index, Comparación de Períodos de Cierre y Analytics Dinámico**<br><br>Cambio de la eliminación física de asignaciones de impresoras por desactivación lógica (`is_active = False`) para preservar el `entry_index` del usuario en la libreta de direcciones de la impresora y evitar colisiones futuras al reaprovisionar. Renombramiento del botón de "Eliminar" a "Desactivar en este equipo" en la interfaz de usuario. Desarrollo e integración en la pestaña de Consumo de Usuarios de Analytics de la funcionalidad de comparación de períodos de cierre, permitiendo al administrador comparar visualmente los consumos de un mes contra otro. Reemplazo de los períodos de meses estáticos (mock) por selectores dinámicos que consultan los cierres únicos existentes en la base de datos (`uniquePeriods`). | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 03/07/26 | 06/07/26 | **Entregable / Código / Frontend:** Modificación de la lógica de desactivación en `users.py` (soft delete preservando `entry_index`). Componentes de Analytics actualizados: `ConsumoPorUsuario.tsx`, `ResumenGeneral.tsx`. Selectores dinámicos consultando endpoint `/closures/periods`. | La desactivación física (DELETE en la tabla de asignaciones) causaba la pérdida del `entry_index` original, lo cual generaba colisiones al reaprovisionar al usuario en la misma impresora con un nuevo índice. Se solucionó migrando a desactivación lógica que conserva el registro intacto. |
| **Optimización UX de Trabajos de Impresión: Responsividad, Paginación Interactiva, Multi-selección de Impresoras y Control de Calidad (QA)**<br><br>Eliminación del scroll horizontal residual en la tabla de Trabajos de Impresión mediante la reducción de paddings, anchos de columna y remoción de la columna irrelevante "ID Trabajo". Expansión del contenedor principal a ancho fluido (`w-full`) para aprovechar la totalidad del espacio horizontal en monitores Full HD. Implementación de un sistema de paginación interactivo duplicado (superior e inferior) con selectores dinámicos de elementos por página (10, 25, 50, 100), navegación numérica reactiva y totalizador de registros. Desarrollo de la capacidad de multi-selección de impresoras con checkboxes para consultar trabajos de impresión en varias impresoras simultáneamente usando `ThreadPoolExecutor` en paralelo. Ejecución de un reporte de control de calidad visual verificando la correcta visualización de la paginación, el filtrado, y la tabla en producción. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 07/07/26 | 07/07/26 | **Entregable / Código / Frontend / QA:** Modificaciones en `PrintJobsPage.tsx` (paginación, multi-select, layout fluido). Backend: `api/stored_jobs.py` (consulta paralela multi-impresora). Documentación técnica: `FIX_UX_RESPONSIVO_Y_SPACING_TRABAJOS_IMPRESION_07_JULIO_2026.md`. Reporte de QA: `REPORTE_QA_UX_Y_PAGINACION_TRABAJOS_IMPRESION_07_JULIO_2026.md`. | La suma de anchos fijos de las 8 columnas de la tabla más los paddings horizontales de 48px por columna excedía los ~1600px de espacio útil en escritorio tras descontar los 240px del sidebar, generando la barra de scroll horizontal gris. Se solucionó con la reducción coordinada de paddings a `px-4` (16px) y ajuste de anchos de columna. |
| **Portal de Auditoría de Seguridad Independiente (Puerto 8088) con SQLite, Bcrypt y JWT**<br><br>Diseño e implementación de un microservicio de auditoría de seguridad completamente aislado que corre en el puerto `8088` del servidor. El portal almacena los logs de seguridad (inicios de sesión, refrescos de tokens, validaciones) en una base de datos SQLite local independiente para no degradar el rendimiento de PostgreSQL. Implementación de autenticación propia basada en bcrypt con tokens JWT de 30 minutos de expiración. Desarrollo de la interfaz web del portal con listado cronológico de fechas con logs, buscador en vivo por IP/Ejecutor/Acción, visor de JSON estructurado y exportación a CSV. Paginación server-side con `LIMIT` y `OFFSET` para garantizar carga instantánea de la bitácora del día. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 07/07/26 | 07/07/26 | **Entregable / Código / Servidor / Seguridad:** Módulo de auditoría `backend/api/audit_portal.py` con SQLite y bcrypt. Script de generación de hashes `deployment/generate_bcrypt_hash.py`. Volumen Docker persistente `ricoh-backend-logs`. Documentación técnica: `PORTAL_AUDITORIA_SEGURIDAD_INDEPENDIENTE_JULIO_2026.md`. | Los signos de dólar (`$`) del hash bcrypt generaban conflictos con la expansión de variables de Docker Compose. Se solucionó duplicando los signos (`$$`) en el archivo `.env` del servidor. Se proveyó un script utilitario que realiza este escape automáticamente al generar el hash para producción. |
| **Resolución Definitiva de Compatibilidad de Contraseña de Escáner SMB y Permisos de Color en Impresoras Ricoh**<br><br>Auditoría detallada de las peticiones HTTP/POST del flujo de contraseña de carpeta WIM, identificando que los modelos de impresora más recientes ignoran los campos `wkpasswordIn` y requieren `passwordIn` para la inyección de la clave Base64. Implementación del envío dual de la contraseña en ambas variantes de campo para garantizar compatibilidad universal. Corrección del parámetro `isFolderAuthPasswordUpdated` de `true` a `false` en el guardado final para evitar el borrado silencioso de la contraseña temporal por parte del firmware. Restricción de las opciones de color intermedio `TC` (Dos colores) y `MC` (Color personalizado) al flag `copiadora_color` para evitar permisos de color no deseados en usuarios de solo Blanco y Negro. Incremento de reintentos de sincronización de 4 a 12 para impresoras ocupadas. Instanciación de `RicohWebClient` aislados por hilo worker para garantizar thread-safety en operaciones de aprovisionamiento paralelo. Verificación física exitosa del escáner en impresoras `192.168.91.250` y `192.168.91.252` con ruta de red `\\TIC0264\Escaner`. | Establecer requisitos de la solución de software de acuerdo con estándares y procedimiento técnico. | 07/07/26 | 08/07/26 | **Entregable / Código / Servidor / Pruebas Físicas:** Modificaciones en `ricoh_password_flow.py` (envío dual de campos de contraseña, `isFolderAuthPasswordUpdated=false`). Modificaciones en `ricoh_web_client.py` (restricción TC/MC a `copiadora_color`, thread-safety con instancias aisladas, 12 reintentos). Frontend: `ModificarUsuario.tsx` (notificaciones coherentes de éxito). Documentación técnica: `FIX_RESOLUCION_COMPATIBILIDAD_CONTRASENA_ESCANER_Y_PERMISOS_COLOR_08_JULIO_2026.md`. | Al enviar los permisos/carpeta desde el sistema web, el escáner a carpeta local (SMB) dejaba de funcionar arrojando "Error de Autenticación", pero manualmente mediante Web Image Monitor sí funcionaba. La causa raíz fue que el backend enviaba la contraseña en campos que los modelos nuevos de Ricoh ignoran (`wkpasswordIn`) y además el flag `isFolderAuthPasswordUpdated=true` provocaba que el firmware sobrescribiera la clave temporal con una vacía al guardar. Se corrigió con el envío dual y el cambio del flag a `false`. |
